[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "occubed"
version = "1.0.0"
description = "Active Continuous-Score Occupancy Modeling."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]